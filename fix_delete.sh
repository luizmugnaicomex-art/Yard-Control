sed -i 's/onClick={() => setDepots(depots.filter(d => d.id !== depot.id))}/onClick={() => {\n                                          setDepots(depots.filter(d => d.id !== depot.id));\n                                          setDepotMatrix(prev => {\n                                            const newMatrix = { ...prev };\n                                            delete newMatrix[depot.name];\n                                            return newMatrix;\n                                          });\n                                        }}/g' src/App.tsx

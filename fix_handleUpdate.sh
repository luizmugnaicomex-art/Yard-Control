sed -i 's/const handleUpdate = (field: keyof Depot, value: string | number) => {/const handleUpdate = (field: keyof Depot, value: string | number) => {\n                                  const oldName = depots[idx].name;/g' src/App.tsx
sed -i 's/setDepots(newDepots);/setDepots(newDepots);\n                                  if (field === '\''name'\'' \&\& typeof value === '\''string'\'') {\n                                    setDepotMatrix(prev => {\n                                      const newMatrix = { ...prev };\n                                      if (newMatrix[oldName]) {\n                                        newMatrix[value] = newMatrix[oldName];\n                                        delete newMatrix[oldName];\n                                      }\n                                      return newMatrix;\n                                    });\n                                  }/g' src/App.tsx
